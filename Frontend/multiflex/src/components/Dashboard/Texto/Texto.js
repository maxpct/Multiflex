import './Texto.css';

// Este componente muestra el texto que va debajo del título.
function Texto() {
  return <p className="dashboard-texto">Resumen de las solicitudes de servicio registradas en el sistema.</p>;
}

export default Texto;
